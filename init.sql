-- init.sql
CREATE DATABASE IF NOT EXISTS fibrastar_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE fibrastar_db;

-- Si tienes INSERTS o estructura de tablas que quieras preservar, pégalos aquí abajo.
-- Ejemplo:
-- INSERT INTO planes (nombre, costo, ...) VALUES ...;